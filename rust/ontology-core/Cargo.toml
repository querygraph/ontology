[package]
name = "ontology-core"
description = "Framework-neutral topic-ontology engineering for Rust backends: durable normalization with cross-language key parity, the shared cold-start seed, and exact-key topic extraction"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
unicode-normalization = "0.1"
